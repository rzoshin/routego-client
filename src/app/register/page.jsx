import RegisterPage from '@/components/auth/RegisterPage';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegisterPage />
        </div>
    );
};

export default page;